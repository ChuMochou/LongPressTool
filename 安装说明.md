# 安装说明

## 一、APK 在哪

```
LongPressTool-v1.0-release.apk        （项目根目录，1.76 MB）
```

这是 **release 正式签名版**，属性如下：

| 项目 | 值 |
|---|---|
| 应用名 | 长按器 |
| 包名 | `com.example.longpresstool` |
| 版本 | 1.0（versionCode 1） |
| 最低支持 | Android 7.0（API 24） |
| 目标版本 | Android 16（API 37） |
| 签名 | v2 方案（Android 7.0+ 均可安装），RSA 2048 |
| 签名指纹 SHA-256 | `7914eaf096056f43fcf8c190564da7de93f57e9806eda0068d22e4f9e2fa4855` |

> debug 包（`app-debug.apk`）用的是 Android Studio 的调试密钥，体积 16.5 MB 且带调试标记，
> 日常使用请用上面这个 release 包。

---

## 二、安装步骤

1. 把 `LongPressTool-v1.0-release.apk` 传到手机（数据线 / 微信文件传输 / 网盘均可）
2. 在手机上点击该文件安装
3. 系统会提示"未知来源应用"，允许一次即可（个人自用，无需上架）

> **如果之前装过 debug 版**：必须先卸载旧版再装这个，因为两者签名不同，无法覆盖安装。
> 卸载会清掉已保存的准星位置和权限（重装后需要重新开启下面两个权限）。

---

## 三、首次使用：开启两个权限

装好后打开 App，首页会显示缺哪个权限，并给出直达系统设置的按钮。

### 1. 显示在其他应用上层

**设置 → 应用 → 长按器 → 显示在其他应用上层 → 允许**

### 2. 无障碍服务

**设置 → 无障碍 → 已下载的应用 → 长按器 → 打开**

#### ⚠️ 如果无障碍开关点不动

Android 13 及以上，对**手动安装的 APK（侧载应用）**默认禁止开启无障碍服务。
表现为：列表里能看到「长按器」，但开关点不动或自己弹回去。

解决办法：

1. **设置 → 应用 → 长按器**
2. 点右上角 **三个点**
3. 选 **「允许受限设置」**（Allow restricted settings）并确认
4. 回到 **设置 → 无障碍 → 已下载的应用 → 长按器**，这时就能打开开关了

---

## 四、怎么用

1. 点首页「**启动长按器**」
2. 屏幕上出现两个悬浮窗：
   - **控制侧边栏**（可拖动）：显示当前准星坐标，含「启动」「停止」「关闭」
   - **圆形准星**（可拖动）：对准你想长按的位置
3. 拖动准星到目标位置（例如某个按钮、聊天消息、桌面图标）
4. 点「**启动**」→ 该位置开始持续按住不放
   - 准星变红 + 外圈出现转动的虚线环和扩散波纹
   - 侧边栏状态灯变红
5. 点「**停止**」→ 立刻松手
6. 点侧边栏「**关闭**」（或首页的「关闭」）→ 结束长按并移除两个悬浮窗
7. 首页「**退出**」→ 关闭悬浮窗并退出应用

> 侧边栏或准星的位置会被记住，下次打开还在原处。

---

## 五、注意事项

- **侧边栏不要压在长按目标上**：侧边栏必须保持可点击（否则按不了「停止」），
  所以万一它正好盖住目标位置，注入的触摸会被它接收。App 检测到这种情况会提示你把它拖开。
- **长按期间会看到一条常驻通知**：这是 Android 对前台 Service 的强制要求，
  没有它系统会随时回收进程、悬浮窗就消失了。
- **长按中用手指碰屏幕会打断长按**：这是系统行为（用户操作优先于注入的手势）。
- **一次连续按压上限约 1 分钟**：Android 对单次手势有 60 秒硬限制。
  到时间后 App 会自动接力下一笔，实测间隔约 65ms，正常使用察觉不到。
- **Android 8.0 以下**：系统不支持手势接力，单次长按最多 60 秒，
  App 会明确提示而不是假装能用。

---

## 六、自己重新打包

### 前提：签名密钥

`keystore/` 目录和 `keystore.properties` **已被 .gitignore 忽略**，不会提交到 Git。
如果换电脑或重新 clone，需要把这两个一起拷过来，否则新包签名不同、无法覆盖安装。

- 密钥库：`keystore/longpresstool.jks`
- 密码信息：`keystore.properties`（`storePassword` / `keyAlias` / `keyPassword`）
- 有效期：30 年

### 打包命令

```powershell
# 正式包（R8 压缩 + 正式签名）
.\gradlew.bat :app:assembleRelease
# 产物：app\build\outputs\apk\release\app-release.apk

# 调试包
.\gradlew.bat :app:assembleDebug

# 单元测试
.\gradlew.bat :app:testDebugUnitTest

# 静态检查
.\gradlew.bat :app:lintDebug
```

### 修改版本号

改 `app/build.gradle.kts` 里的 `versionCode` 和 `versionName`，
然后重新 `assembleRelease`。覆盖安装时 `versionCode` 必须比已装的版本大。
